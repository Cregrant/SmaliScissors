plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    //compile group: 'com.google.re2j', name: 're2j', version: '1.6'
}

sourceSets {

tasks.jar {
    manifest.attributes["Main-Class"] = "com.github.cregrant.smaliscissors.Main"
    archiveBaseName.set("patcher")
    destinationDirectory.set(rootDir)
    delete(archiveFile.get())
}
